# Netbeans excludes
# The dist folder is allowed so that we can release compiled binaries - if you are making a pull request, just don't include any libraries and don't delete the readme file
/nbproject/private/
/build/
manifest.mf

# Eclipse excludes
.project
.classpath
/bin/
/.settings/

# OS generated files
.DS_Store
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db